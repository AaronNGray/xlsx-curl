{
  "name": "xlsx-curl",
  "version": "0.0.1",
  "description": "curl .xlsx fetch",
  "main": "curl.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AaronNGray",
  "license": "MIT",
  "dependencies": {
    "curl": "^0.1.4"
  }
}
